import 'package:flutter/material.dart';

class PerscriptionRoute extends StatelessWidget {
  const PerscriptionRoute({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Perscription Page"),
      ),
    );
  }
}
